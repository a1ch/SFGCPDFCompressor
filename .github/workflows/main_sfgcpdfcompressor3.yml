# DISABLED - sfgcpdfcompressor3 is being replaced by sfgcpdfcompressor4 (Container Image mode)
name: '[DISABLED] Deploy to sfgcpdfcompressor3'

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is disabled. Use the sfgcpdfcompressor4 workflow instead.'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. sfgcpdfcompressor3 has been replaced by sfgcpdfcompressor4."
