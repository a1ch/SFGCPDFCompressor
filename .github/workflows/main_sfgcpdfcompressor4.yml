# DISABLED - sfgcpdfcompressor4 retired, use main_sfgcpdfcompressor5.yml
name: DISABLED - sfgcpdfcompressor4

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled - app retired"
