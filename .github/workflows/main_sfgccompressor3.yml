# DISABLED - superseded by main_sfgcpdfcompressor3.yml (Docker + Ghostscript build)
# This workflow is intentionally set to manual-only and will not run on push.
name: '[DISABLED] Old deploy - sfgccompressor3'

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'This workflow is disabled. Use main_sfgcpdfcompressor3.yml instead.'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. Use main_sfgcpdfcompressor3.yml instead."
