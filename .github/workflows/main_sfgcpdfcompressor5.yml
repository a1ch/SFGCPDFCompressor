# DISABLED - retired, all deploys now go through SFGCPDFCompressorPy repo
name: DISABLED - sfgcpdfcompressor5

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled - use SFGCPDFCompressorPy repo"
